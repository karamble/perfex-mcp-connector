# --- Secrets / credentials (never commit) ---
.env
.env.*
*.env
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*
secrets*
*secret*
credentials*
config.local.php
app-config.php

# --- Claude Code / AI assistant artifacts (never commit) ---
.claude/
.claude.json
.claude.json.backup
.mcp.json
CLAUDE.md

# --- Local dev / scratch ---
/dev/tmp/
*.log
.DS_Store

# --- Build artifacts ---
/dist/

# Note: mcp_connector/vendor/ IS intentionally committed (the bundled MCP SDK,
# shipped inside the module zip). It contains no secrets.
